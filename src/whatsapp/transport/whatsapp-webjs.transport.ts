// Compatibility name for the existing, fully tested whatsapp-web.js adapter.
// The implementation remains in place during the parallel migration window.
export { WhatsAppClientService as WhatsAppWebJsTransport } from '../whatsapp-client.service';
